{
  "name": "safebite-ai",
  "private": true,
  "scripts": {
    "dev": "concurrently --prefix-colors \"cyan,magenta\" --names \"CLIENT,SERVER\" \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "setup": "npm install && npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
